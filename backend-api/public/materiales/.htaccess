# Deshabilitar listado de archivos
Options -Indexes

# Permitir acceso solo a archivos específicos
<FilesMatch "\.(pdf|doc|docx|xls|xlsx|ppt|pptx|zip|rar)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
