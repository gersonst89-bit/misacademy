{
  "name": "mis-academy",
  "private": true,
  "description": "Monorepo — MIS Academy Platform",
  "scripts": {
    "backend": "cd backend-nestjs && npm run start:dev",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd backend-nestjs && npm install && cd ../frontend && npm install"
  }
}
